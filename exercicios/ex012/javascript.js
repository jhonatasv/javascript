function star () {
    let numero = Number(prompt('Digite um número:'))
    // let resultado = numero <  ? 'impar' : 'par'
    document.querySelector('#numero').innerHTML = `O numero ${numero} que foi digitado é ${resultado}`
}